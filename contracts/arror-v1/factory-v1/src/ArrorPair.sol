pragma solidity ^0.8.17;

contract ArrorPair{
        
        function addLiquidity(address account, uint256 amount) public {
        
        }   
    
        function removeLiquidity(address account, uint256 amount) public {
        
        }
}